language: R
sudo: false
cache:
  packages: yes
  directories:
    - docs


before_install:
  - Rscript -e "if (!require('devtools')) install.packages('devtools')"
  - Rscript -e "devtools::update_packages()"

script:
  - make

